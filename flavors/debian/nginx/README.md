# Debian Nginx flavor

This flavor installs the stock Debian `nginx` package without additional web
server configuration.

```bash
./flavors/build.sh debian nginx bookworm
```

The output is `bookworm-generic-amd64-qa.nginx.qcow2`.
