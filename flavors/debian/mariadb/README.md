# Debian MariaDB flavor

This flavor installs the stock Debian `mariadb-server` package without
additional database or server configuration.

```bash
./flavors/build.sh debian mariadb bookworm
```

The output is `bookworm-generic-amd64-qa.mariadb.qcow2`.
